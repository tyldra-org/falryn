/** Public contracts for this capability. Internal modules import their exact dependencies. */

export { planGitCommits } from "./commit-plan.ts";
export type {
  CommitChangeState,
  CommitChangeUnit,
  CommitGroup,
  CommitPlan,
  CommitPlanProvenance,
  CommitPlanValidation,
  CommitUnassigned,
  GitBlameLine,
  GitBlameRequest,
  GitBlameSnapshot,
  GitBranchMutation,
  GitBranchMutationKind,
  GitChangeKind,
  GitCheckpointList,
  GitCheckpointRecord,
  GitCheckpointSnapshot,
  GitCheckpointUntracked,
  GitCommitPlanSnapshot,
  GitCommitRequest,
  GitCommitResult,
  GitCreateBranchRequest,
  GitCreateCheckpointRequest,
  GitCreateWorktreeRequest,
  GitDeleteBranchRequest,
  GitDiffRequest,
  GitDiffScope,
  GitDiffSnapshot,
  GitDiscoverRequest,
  GitError,
  GitExpectedHeadRequest,
  GitFetchRequest,
  GitField,
  GitHeadState,
  GitIdentity,
  GitIndexMutation,
  GitListCheckpointsRequest,
  GitListWorktreesRequest,
  GitLogCommit,
  GitLogRequest,
  GitLogSnapshot,
  GitOperationState,
  GitPlanCommitsRequest,
  GitPort,
  GitPullRequest,
  GitPushRequest,
  GitRemote,
  GitRemoteResult,
  GitRemoveWorktreeRequest,
  GitRequestBase,
  GitRestoreCheckpointRequest,
  GitRestorePlan,
  GitRestoreResult,
  GitStageRequest,
  GitStatusEntry,
  GitStatusRequest,
  GitStatusSnapshot,
  GitSwitchBranchRequest,
  GitSyncRequest,
  GitSyncResult,
  GitUnstageRequest,
  GitWorktreeList,
  GitWorktreeMutation,
  GitWorktreeMutationKind,
  GitWorktreeRecord,
  ParsedGitRequest,
} from "./git.ts";
export {
  COMMIT_CHANGE_STATES,
  COMMIT_PLAN_SOURCE,
  COMMIT_PLAN_VERSION,
  classifyGitStderr,
  DEFAULT_GIT_BLAME_LINES,
  DEFAULT_GIT_CHECKPOINTS,
  DEFAULT_GIT_DIFF_BYTES,
  DEFAULT_GIT_LOG_COMMITS,
  DEFAULT_GIT_REMOTE_NAME,
  DEFAULT_GIT_STATUS_ENTRIES,
  DEFAULT_GIT_TIMEOUT_MS,
  DEFAULT_GIT_WORKTREES,
  formatGitCheckpointMessage,
  GIT_BRANCH_MUTATION_KINDS,
  GIT_CHANGE_KINDS,
  GIT_CHECKPOINT_REF_PREFIX,
  GIT_CHECKPOINT_VERSION,
  GIT_DIFF_SCOPES,
  GIT_HEAD_STATES,
  GIT_INVOCATION_PREFIX,
  GIT_OBSERVATION_ENVIRONMENT,
  GIT_OPERATION_STATES,
  GIT_USER_HOOK_INVOCATION_PREFIX,
  GIT_WORKTREE_MUTATION_KINDS,
  gitArgv,
  gitCheckpointRef,
  gitFailureFromCapture,
  gitFailureFromStop,
  gitUserHookArgv,
  indexHasStagedChanges,
  isSecretPath,
  MAX_COMMIT_PLAN_GROUPS,
  MAX_GIT_BLAME_LINES,
  MAX_GIT_CHECKPOINT_REFERENCE_LENGTH,
  MAX_GIT_CHECKPOINT_UNTRACKED,
  MAX_GIT_CHECKPOINTS,
  MAX_GIT_COMMIT_SUBJECT_LENGTH,
  MAX_GIT_DIFF_BYTES,
  MAX_GIT_LOG_COMMITS,
  MAX_GIT_REF_NAME_LENGTH,
  MAX_GIT_REMOTE_URL_LENGTH,
  MAX_GIT_STAGE_PATHS,
  MAX_GIT_STATUS_ENTRIES,
  MAX_GIT_WORKTREES,
  parseGitBlame,
  parseGitCheckpointMessage,
  parseGitCheckpointRefs,
  parseGitLog,
  parseGitRemotes,
  parseGitVersion,
  parseGitWorktrees,
  parseRevParsePaths,
  parseStatusPorcelainV2,
  redactGitRemoteUrl,
  refuseUnsafeGitIdentity,
  stagedSecretPath,
  validateGitBlameRequest,
  validateGitCheckpointLimits,
  validateGitCheckpointReference,
  validateGitCommitSubject,
  validateGitDiffLimits,
  validateGitIncludeUntracked,
  validateGitLogLimits,
  validateGitOid,
  validateGitPathspecs,
  validateGitRefName,
  validateGitRelPath,
  validateGitRemoteName,
  validateGitRequest,
  validateGitRevision,
  validateGitStagePaths,
  validateGitStatusLimits,
  validateGitWorktreeLimits,
  worktreeHasBlockingChanges,
} from "./git.ts";
