---
name: github-workflow
description: >-
  Deprecated alias. Load git-workflow for git, gh-cli for GitHub, origin-cli
  for Cursor Origin. Kept so old attachments still resolve.
disable-model-invocation: true
---

# Deprecated: github-workflow

This skill was renamed and split. Load the matching skill instead:

| Work | Skill |
| --- | --- |
| `git` commit, branch, sync, rebase, rewrite, recover | **git-workflow** (`~/.agents/skills/git-workflow/SKILL.md`) |
| GitHub `gh` — issues, PRs, Actions, merge | **gh-cli** (`~/.agents/skills/gh-cli/SKILL.md`) |
| Cursor Origin `origin` — PRs, mirrors, rulesets | **origin-cli** (`~/.agents/skills/origin-cli/SKILL.md`) |

Do not follow this file as procedure.
